# Checkmarx CI Integration - Python dependencies
#
# Note: This script uses only Python standard library modules:
#   - smtplib, email (SMTP and HTML email)
#   - subprocess (running the cx CLI)
#   - json, os, sys, argparse, pathlib, datetime (basic utilities)
#
# No external pip packages are required. Python 3.7+ is sufficient.
#
# This file is intentionally empty (just comments) but kept for convention.
