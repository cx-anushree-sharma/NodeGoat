# Fuzzing-only dependencies for fuzz_validate_config.py.
#
# Deliberately kept separate from the top-level requirements.txt, which
# documents that validate_config.py itself has zero external dependencies.
# atheris ships prebuilt wheels for Linux and macOS only; it does not
# install on Windows.
atheris==3.1.0 \
    --hash=sha256:f8a9f51ce8369026e8eb7b7174835e8c4c85a1a6db5d9add36c15100779d2a39
